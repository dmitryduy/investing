import styled from "styled-components";

export const Hint = styled.span`
  color: rgba(0, 0, 0, .54);
  font-size: 11px;
  letter-spacing: -.2px;
  margin-left: 10px;
  transition: .2s;

  &.animate {
    color: #c40b08;
  }
`;